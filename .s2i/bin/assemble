echo "--> assemble refer script started !!"
echo "--> mv /tmp/src/myweb /tmp/src/ROOT.war !!"
mv /tmp/src/myweb /tmp/src/ROOT.war
#touch /deployments/ROOT.war.dodeploy
#echo "--> /deployments/ROOT.war.dodeploy file is touched !!"
#echo "--> mv /tmp/src/gpki /home/jboss/gpki !!"
#mv /tmp/src/gpki /home/jboss/gpki
echo "$NB_USER $uid $user $home"
cat $NSS_WRAPPER_PASSWD
/usr/local/s2i/assemble
exit $?
